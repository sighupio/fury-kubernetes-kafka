---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  labels:
    grafana-sighup-dashboard: kafka

configMapGenerator:
  - name: kafka-default
    files:
      - strimzi-kafka.json
  - name: kafka-operator
    files:
      - strimzi-operators.json
  - name: kafka-zookeeper
    files:
      - strimzi-zookeeper.json
